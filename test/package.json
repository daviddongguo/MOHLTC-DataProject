{
	"name": "test",
	"version": "1.0.0",
	"description": "",
	"main": "main.js",
	"scripts": {
		"test": "mocha main.js users packages --recursive --exit --check-leaks -R mochawesome",
		"test:david": "nodemon --exec mocha --recursive --timeout 5000 -R min"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"chai": "^4.2.0",
		"chai-http": "^4.3.0",
		"mocha": "^8.2.1",
		"mochawesome": "^6.2.1"
	}
}
